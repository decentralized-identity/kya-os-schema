{
  "name": "@kya-os/schema",
  "version": "0.0.0",
  "description": "KYA-OS protocol schema definitions and JSON Schema generator (source of truth)",
  "type": "module",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "gen": "node scripts/emit-schemas.mjs",
    "drift": "node scripts/drift-gate.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "vitest": "3.1.1",
    "@kya-os/mcp": "1.4.0"
  }
}
