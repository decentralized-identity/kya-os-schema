# Default owners for everything in the repo.
# NOTE(transfer): org-scoped teams (@kya-os/*) silently stop resolving after
# the repo transfers to decentralized-identity, so ownership is pinned to
# individual accounts until DIF-org teams exist. Re-team once
# decentralized-identity/kya-os-schema has its maintainer teams provisioned.
*                       @H0BB5

# The protocol-core source of truth and the published schema tree.
/packages/protocol-core/ @H0BB5
/schemas/               @H0BB5

# The specification site.
/site/                  @H0BB5
